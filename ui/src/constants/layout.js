// Sidebar / drawer width shared between the persistent pinned sidebar
// (NavBar.jsx) and the layout that reserves space for it (App.jsx), so the
// two don't drift out of sync via a duplicated magic number. Matches Joy
// Drawer's 'sm' horizontal size so the pinned sidebar lines up visually with
// the overlay drawer it replaces.
export const PINNED_DRAWER_WIDTH = 256
